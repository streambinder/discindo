language: bash

commands:

    clean:
        description: Clean up
        exec: python3 setup.py clean --all

    install:
        description: Install binary
        dependencies:
            - build
        exec: python3 setup.py install

    build:
        description: Build binary
        exec: sudo python3 setup.py build